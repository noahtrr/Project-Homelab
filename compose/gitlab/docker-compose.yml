name: gitlab
services:
  gitlab:
    image: gitlab/gitlab-ce:latest-ce.0
    ports:
      - "8084:80"
      - "8443:443"
      - "822:22"
    restart: unless-stopped
    container_name: template
    hostname: gitlab
    volumes:
      - ./config:/etc/gitlab
      - ./logs:/var/log/gitlab
      - ./data:/var/opt/gitlab
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        # Add any other gitlab.rb configuration here, each on its own line
        external_url 'https://gitlab.ojos-sole.ts.net'
        gitlab_rails['gitlab_shell_ssh_port'] = 822
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "gitlab"
      tsdproxy.autodetect: "false"
      tsdproxy.port: "8443"
      tsdproxy.dash.icon: "sh/gitlab"
      glance.name: Gitlab
      glance.icon: sh:gitlab
      glance.url: https://gitlab.ojos-sole.ts.net
      glance.description: gitlab
      glance.id: gitlab
    networks:
      - proxy
    shm_size: '256m'

networks:
  proxy:
    external: true